# 2-5. ロードバランサーとDNS

AWSのロードバランサー（ALB、NLB）とDNSサービス（Route 53、CloudFront）の構築方法を説明します。

## 2-5-1. Application Load Balancer（ALB）

### ALBの基本作成

Application Load Balancerを作成します。

```hcl
resource "aws_lb" "main" {
  name               = "main-alb"
  internal           = false
  load_balancer_type = "application"
  security_groups    = [aws_security_group.alb.id]
  subnets            = [
    aws_subnet.public_1a.id,
    aws_subnet.public_1c.id
  ]
  
  enable_deletion_protection = false
  
  tags = {
    Name = "main-alb"
  }
}
```

### リスナーの設定

ALBのリスナーを設定します。

```hcl
# HTTPリスナー
resource "aws_lb_listener" "http" {
  load_balancer_arn = aws_lb.main.arn
  port              = 80
  protocol          = "HTTP"
  
  default_action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.main.arn
  }
}

# HTTPSリスナー
resource "aws_lb_listener" "https" {
  load_balancer_arn = aws_lb.main.arn
  port              = 443
  protocol          = "HTTPS"
  ssl_policy        = "ELBSecurityPolicy-2016-08"
  certificate_arn   = aws_acm_certificate.main.arn
  
  default_action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.main.arn
  }
}
```

### ターゲットグループの作成

ターゲットグループを作成します。

```hcl
resource "aws_lb_target_group" "main" {
  name     = "main-tg"
  port     = 80
  protocol = "HTTP"
  vpc_id   = aws_vpc.main.id
  
  health_check {
    enabled             = true
    healthy_threshold   = 2
    unhealthy_threshold = 2
    timeout             = 5
    interval            = 30
    path                = "/health"
    matcher             = "200"
  }
  
  tags = {
    Name = "main-target-group"
  }
}

# ターゲットの登録
resource "aws_lb_target_group_attachment" "main" {
  target_group_arn = aws_lb_target_group.main.arn
  target_id        = aws_instance.web.id
  port             = 80
}
```

### ヘルスチェックの設定

ヘルスチェックを詳細に設定します。

```hcl
resource "aws_lb_target_group" "main" {
  name     = "main-tg"
  port     = 80
  protocol = "HTTP"
  vpc_id   = aws_vpc.main.id
  
  health_check {
    enabled             = true
    healthy_threshold   = 3      # 正常と判断するまでの連続成功回数
    unhealthy_threshold = 3      # 異常と判断するまでの連続失敗回数
    timeout             = 5      # タイムアウト（秒）
    interval            = 30     # ヘルスチェック間隔（秒）
    path                = "/health"
    protocol            = "HTTP"
    matcher             = "200-299"  # 正常と判断するHTTPステータスコード
  }
}
```

### SSL/TLS証明書の設定（ACM）

ACMで証明書を作成してALBに設定します。

```hcl
# ACM証明書の作成
resource "aws_acm_certificate" "main" {
  domain_name       = "example.com"
  validation_method = "DNS"
  
  subject_alternative_names = [
    "*.example.com"
  ]
  
  lifecycle {
    create_before_destroy = true
  }
  
  tags = {
    Name = "main-certificate"
  }
}

# DNS検証レコードの作成
resource "aws_route53_record" "cert_validation" {
  for_each = {
    for dvo in aws_acm_certificate.main.domain_validation_options : dvo.domain_name => {
      name   = dvo.resource_record_name
      record = dvo.resource_record_value
      type   = dvo.resource_record_type
    }
  }
  
  allow_overwrite = true
  name            = each.value.name
  records         = [each.value.record]
  ttl             = 60
  type            = each.value.type
  zone_id         = aws_route53_zone.main.zone_id
}

# 証明書の検証
resource "aws_acm_certificate_validation" "main" {
  certificate_arn         = aws_acm_certificate.main.arn
  validation_record_fqdns = [for record in aws_route53_record.cert_validation : record.fqdn]
}

# HTTPSリスナーに証明書を設定
resource "aws_lb_listener" "https" {
  load_balancer_arn = aws_lb.main.arn
  port              = 443
  protocol          = "HTTPS"
  ssl_policy        = "ELBSecurityPolicy-TLS-1-2-2017-01"
  certificate_arn   = aws_acm_certificate_validation.main.certificate_arn
  
  default_action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.main.arn
  }
}
```

### リダイレクトルールの設定

HTTPからHTTPSへのリダイレクトを設定します。

```hcl
resource "aws_lb_listener" "http" {
  load_balancer_arn = aws_lb.main.arn
  port              = 80
  protocol          = "HTTP"
  
  default_action {
    type = "redirect"
    
    redirect {
      port        = "443"
      protocol    = "HTTPS"
      status_code = "HTTP_301"
    }
  }
}
```

### 固定レスポンスの設定

固定レスポンスを返すルールを設定します。

```hcl
resource "aws_lb_listener_rule" "fixed_response" {
  listener_arn = aws_lb_listener.https.arn
  priority     = 100
  
  action {
    type = "fixed-response"
    
    fixed_response {
      content_type = "text/plain"
      message_body = "Service Unavailable"
      status_code  = "503"
    }
  }
  
  condition {
    path_pattern {
      values = ["/maintenance"]
    }
  }
}
```

### 認証の設定（Cognito、OIDC）

#### Cognito認証

```hcl
resource "aws_lb_listener_rule" "cognito_auth" {
  listener_arn = aws_lb_listener.https.arn
  priority     = 100
  
  action {
    type = "authenticate-cognito"
    
    authenticate_cognito {
      user_pool_arn       = aws_cognito_user_pool.main.arn
      user_pool_client_id = aws_cognito_user_pool_client.main.id
      user_pool_domain    = aws_cognito_user_pool_domain.main.domain
    }
  }
  
  action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.main.arn
  }
  
  condition {
    path_pattern {
      values = ["/app/*"]
    }
  }
}
```

#### OIDC認証

```hcl
resource "aws_lb_listener_rule" "oidc_auth" {
  listener_arn = aws_lb_listener.https.arn
  priority     = 100
  
  action {
    type = "authenticate-oidc"
    
    authenticate_oidc {
      authorization_endpoint = "https://example.com/oauth2/authorize"
      client_id              = var.oidc_client_id
      client_secret          = var.oidc_client_secret
      issuer                 = "https://example.com"
      token_endpoint         = "https://example.com/oauth2/token"
      user_info_endpoint     = "https://example.com/oauth2/userInfo"
    }
  }
  
  action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.main.arn
  }
  
  condition {
    path_pattern {
      values = ["/secure/*"]
    }
  }
}
```

## 2-5-2. Network Load Balancer（NLB）

### NLBの基本作成

Network Load Balancerを作成します。

```hcl
resource "aws_lb" "main" {
  name               = "main-nlb"
  internal           = false
  load_balancer_type = "network"
  subnets            = [
    aws_subnet.public_1a.id,
    aws_subnet.public_1c.id
  ]
  
  enable_deletion_protection = false
  
  tags = {
    Name = "main-nlb"
  }
}
```

### ターゲットグループの作成

NLB用のターゲットグループを作成します。

```hcl
resource "aws_lb_target_group" "main" {
  name     = "main-nlb-tg"
  port     = 80
  protocol = "TCP"
  vpc_id   = aws_vpc.main.id
  
  health_check {
    enabled             = true
    healthy_threshold   = 3
    unhealthy_threshold = 3
    timeout             = 10
    interval            = 30
    protocol            = "TCP"
  }
  
  tags = {
    Name = "main-nlb-target-group"
  }
}
```

### ヘルスチェックの設定

NLBのヘルスチェックを設定します。

```hcl
resource "aws_lb_target_group" "main" {
  name     = "main-nlb-tg"
  port     = 80
  protocol = "TCP"
  vpc_id   = aws_vpc.main.id
  
  health_check {
    enabled             = true
    healthy_threshold   = 3
    unhealthy_threshold = 3
    timeout             = 10
    interval            = 30
    protocol            = "HTTP"  # HTTPヘルスチェック
    path                = "/health"
    matcher             = "200"
  }
}
```

### 静的IPアドレスの設定

Elastic IPを割り当てます。

```hcl
resource "aws_eip" "nlb_1a" {
  domain = "vpc"
  
  tags = {
    Name = "nlb-eip-1a"
  }
}

resource "aws_eip" "nlb_1c" {
  domain = "vpc"
  
  tags = {
    Name = "nlb-eip-1c"
  }
}

resource "aws_lb" "main" {
  name               = "main-nlb"
  internal           = false
  load_balancer_type = "network"
  
  subnet_mapping {
    subnet_id     = aws_subnet.public_1a.id
    allocation_id = aws_eip.nlb_1a.id
  }
  
  subnet_mapping {
    subnet_id     = aws_subnet.public_1c.id
    allocation_id = aws_eip.nlb_1c.id
  }
}
```

## 2-5-3. Classic Load Balancer（CLB）

### CLBの基本作成（レガシー）

Classic Load Balancerを作成します（非推奨）。

```hcl
resource "aws_elb" "main" {
  name               = "main-clb"
  availability_zones = ["ap-northeast-1a", "ap-northeast-1c"]
  
  listener {
    instance_port     = 80
    instance_protocol = "HTTP"
    lb_port           = 80
    lb_protocol       = "HTTP"
  }
  
  health_check {
    healthy_threshold   = 2
    unhealthy_threshold = 2
    timeout             = 3
    target              = "HTTP:80/"
    interval            = 30
  }
  
  tags = {
    Name = "main-clb"
  }
}
```

### 使用ケースの理解

- **CLB**: レガシー、新規作成は非推奨
- **ALB**: HTTP/HTTPS、パスベースルーティング、ホストベースルーティング
- **NLB**: TCP/UDP、高性能、静的IP

## 2-5-4. Route 53の設定

### ホストゾーンの作成

Route 53のホストゾーンを作成します。

```hcl
resource "aws_route53_zone" "main" {
  name = "example.com"
  
  tags = {
    Name = "main-hosted-zone"
  }
}
```

### Aレコードの作成

Aレコードを作成します。

```hcl
resource "aws_route53_record" "www" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "www.example.com"
  type    = "A"
  ttl     = 300
  records = ["192.0.2.1"]
}
```

### CNAMEレコードの作成

CNAMEレコードを作成します。

```hcl
resource "aws_route53_record" "blog" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "blog.example.com"
  type    = "CNAME"
  ttl     = 300
  records = ["www.example.com"]
}
```

### ALIASレコードの作成

ALBへのALIASレコードを作成します。

```hcl
resource "aws_route53_record" "main" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  alias {
    name                   = aws_lb.main.dns_name
    zone_id                = aws_lb.main.zone_id
    evaluate_target_health = true
  }
}
```

### ヘルスチェックの設定

Route 53のヘルスチェックを設定します。

```hcl
resource "aws_route53_health_check" "main" {
  fqdn              = "example.com"
  port              = 443
  type              = "HTTPS"
  resource_path     = "/health"
  failure_threshold = 3
  request_interval  = 30
  
  tags = {
    Name = "main-health-check"
  }
}
```

### フェイルオーバールーティング

フェイルオーバールーティングを設定します。

```hcl
# プライマリレコード
resource "aws_route53_record" "primary" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "primary"
  
  failover_routing_policy {
    type = "PRIMARY"
  }
  
  alias {
    name                   = aws_lb.primary.dns_name
    zone_id                = aws_lb.primary.zone_id
    evaluate_target_health = true
  }
  
  health_check_id = aws_route53_health_check.primary.id
}

# セカンダリレコード
resource "aws_route53_record" "secondary" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "secondary"
  
  failover_routing_policy {
    type = "SECONDARY"
  }
  
  alias {
    name                   = aws_lb.secondary.dns_name
    zone_id                = aws_lb.secondary.zone_id
    evaluate_target_health = true
  }
}
```

### 地理的位置ベースルーティング

地理的位置に基づいてルーティングします。

```hcl
# アジア向け
resource "aws_route53_record" "asia" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "asia"
  
  geolocation_routing_policy {
    continent = "AS"
  }
  
  alias {
    name                   = aws_lb.asia.dns_name
    zone_id                = aws_lb.asia.zone_id
    evaluate_target_health = true
  }
}

# ヨーロッパ向け
resource "aws_route53_record" "europe" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "europe"
  
  geolocation_routing_policy {
    continent = "EU"
  }
  
  alias {
    name                   = aws_lb.europe.dns_name
    zone_id                = aws_lb.europe.zone_id
    evaluate_target_health = true
  }
}
```

### レイテンシーベースルーティング

レイテンシーに基づいてルーティングします。

```hcl
# 東京リージョン
resource "aws_route53_record" "tokyo" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "tokyo"
  
  latency_routing_policy {
    region = "ap-northeast-1"
  }
  
  alias {
    name                   = aws_lb.tokyo.dns_name
    zone_id                = aws_lb.tokyo.zone_id
    evaluate_target_health = true
  }
}

# バージニアリージョン
resource "aws_route53_record" "virginia" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "virginia"
  
  latency_routing_policy {
    region = "us-east-1"
  }
  
  alias {
    name                   = aws_lb.virginia.dns_name
    zone_id                = aws_lb.virginia.zone_id
    evaluate_target_health = true
  }
}
```

### 重み付けルーティング

重み付けルーティングを設定します。

```hcl
# 90%のトラフィック
resource "aws_route53_record" "main" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "main"
  
  weighted_routing_policy {
    weight = 90
  }
  
  alias {
    name                   = aws_lb.main.dns_name
    zone_id                = aws_lb.main.zone_id
    evaluate_target_health = true
  }
}

# 10%のトラフィック（カナリア）
resource "aws_route53_record" "canary" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  set_identifier = "canary"
  
  weighted_routing_policy {
    weight = 10
  }
  
  alias {
    name                   = aws_lb.canary.dns_name
    zone_id                = aws_lb.canary.zone_id
    evaluate_target_health = true
  }
}
```

## 2-5-5. CloudFront

### CloudFrontディストリビューションの作成

CloudFrontディストリビューションを作成します。

```hcl
resource "aws_cloudfront_distribution" "main" {
  enabled             = true
  is_ipv6_enabled     = true
  comment             = "Main CloudFront Distribution"
  default_root_object = "index.html"
  
  origin {
    domain_name = aws_s3_bucket.main.bucket_regional_domain_name
    origin_id   = "S3-main"
    
    s3_origin_config {
      origin_access_identity = aws_cloudfront_origin_access_identity.main.cloudfront_access_identity_path
    }
  }
  
  default_cache_behavior {
    allowed_methods  = ["GET", "HEAD", "OPTIONS"]
    cached_methods   = ["GET", "HEAD"]
    target_origin_id = "S3-main"
    
    forwarded_values {
      query_string = false
      
      cookies {
        forward = "none"
      }
    }
    
    viewer_protocol_policy = "redirect-to-https"
    min_ttl                = 0
    default_ttl            = 3600
    max_ttl                = 86400
  }
  
  restrictions {
    geo_restriction {
      restriction_type = "none"
    }
  }
  
  viewer_certificate {
    cloudfront_default_certificate = true
  }
  
  tags = {
    Name = "main-cloudfront"
  }
}
```

### オリジンの設定

複数のオリジンを設定します。

```hcl
resource "aws_cloudfront_distribution" "main" {
  # S3オリジン
  origin {
    domain_name = aws_s3_bucket.main.bucket_regional_domain_name
    origin_id   = "S3-main"
    
    s3_origin_config {
      origin_access_identity = aws_cloudfront_origin_access_identity.main.cloudfront_access_identity_path
    }
  }
  
  # ALBオリジン
  origin {
    domain_name = aws_lb.main.dns_name
    origin_id   = "ALB-main"
    
    custom_origin_config {
      http_port              = 80
      https_port             = 443
      origin_protocol_policy = "https-only"
      origin_ssl_protocols   = ["TLSv1.2"]
    }
  }
}
```

### キャッシュビヘイビアの設定

キャッシュビヘイビアを設定します。

```hcl
resource "aws_cloudfront_distribution" "main" {
  # デフォルトキャッシュビヘイビア
  default_cache_behavior {
    target_origin_id       = "S3-main"
    viewer_protocol_policy = "redirect-to-https"
    
    allowed_methods = ["GET", "HEAD", "OPTIONS"]
    cached_methods  = ["GET", "HEAD"]
    
    forwarded_values {
      query_string = false
      cookies {
        forward = "none"
      }
    }
    
    min_ttl     = 0
    default_ttl = 3600
    max_ttl     = 86400
  }
  
  # APIパス用のキャッシュビヘイビア
  ordered_cache_behavior {
    path_pattern     = "/api/*"
    target_origin_id = "ALB-main"
    
    allowed_methods = ["GET", "HEAD", "OPTIONS", "PUT", "POST", "PATCH", "DELETE"]
    cached_methods  = ["GET", "HEAD"]
    
    forwarded_values {
      query_string = true
      headers      = ["Authorization", "Host"]
      
      cookies {
        forward = "all"
      }
    }
    
    viewer_protocol_policy = "https-only"
    min_ttl                = 0
    default_ttl            = 0
    max_ttl                = 0
  }
}
```

### SSL/TLS証明書の設定

カスタムSSL証明書を設定します。

```hcl
resource "aws_cloudfront_distribution" "main" {
  # ...
  
  viewer_certificate {
    acm_certificate_arn      = aws_acm_certificate.cloudfront.arn
    ssl_support_method       = "sni-only"
    minimum_protocol_version = "TLSv1.2_2021"
  }
}

# us-east-1リージョンで証明書を作成（CloudFront用）
resource "aws_acm_certificate" "cloudfront" {
  provider          = aws.us_east_1
  domain_name       = "example.com"
  validation_method = "DNS"
  
  subject_alternative_names = ["*.example.com"]
}
```

### カスタムドメインの設定

カスタムドメインを設定します。

```hcl
resource "aws_cloudfront_distribution" "main" {
  aliases = ["example.com", "www.example.com"]
  
  # ...
}

# Route 53でALIASレコードを作成
resource "aws_route53_record" "cloudfront" {
  zone_id = aws_route53_zone.main.zone_id
  name    = "example.com"
  type    = "A"
  
  alias {
    name                   = aws_cloudfront_distribution.main.domain_name
    zone_id                = aws_cloudfront_distribution.main.hosted_zone_id
    evaluate_target_health = false
  }
}
```
